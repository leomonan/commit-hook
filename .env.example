# commit-hooks LLM API Key 环境变量模板
# 复制为 .env 后填写真实密钥：cp .env.example .env
# 使用前加载：source commit-hooks/.env  或  export $(grep -v '^#' commit-hooks/.env | xargs)

# OpenAI 兼容（OpenAI / DeepSeek / 自建等），对应 commit-hooks.llm.toml 中的 [openai] / [deepseek]
OPENAI_API_KEY=
DEEPSEEK_API_KEY=

# Anthropic，对应 [anthropic]
ANTHROPIC_API_KEY=
